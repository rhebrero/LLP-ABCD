#include "TLorentzVector.h"
#include "TMath.h"
#include <vector>


bool test(
    float test
) {
    return true;
}

bool Vtest(
    vector<double> vector_var
) {
    // Longitud del vector
    return vector_var[0] > 0;
}